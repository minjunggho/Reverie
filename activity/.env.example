# Activity frontend env (copy to .env.local). Everything here is PUBLIC —
# Vite inlines VITE_* values into shipped JS. Secrets belong in backend/.env.

# Force mock mode in dev without ?mock=1 (dev builds only).
VITE_REVERIE_MOCK=0
