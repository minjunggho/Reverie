# Core service
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# Campaign import (schema_version 2.0 YAML contract)
PyYAML>=6.0

# Persistence
SQLAlchemy>=2.0.30
alembic>=1.13
asyncpg>=0.29          # PostgreSQL (production)
aiosqlite>=0.20        # SQLite (tests / local dev)
greenlet>=3.0

# Discord adapter
discord.py>=2.4

# LLM providers (optional at runtime; only the active one is used)
anthropic>=0.40
openai>=1.40
httpx>=0.27

# Testing
pytest>=8.2
pytest-asyncio>=0.23
